<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Canteen Manager - BIAM &copy; 2024</title>
    <link rel="stylesheet" href="css/styles.css">
    <script src="js/script.js"></script>
    <link rel="icon" type="image/png" sizes="32x32" href="fav.png">
</head>
<body>
    <div class="co-container">

        <div class="co-header">
            <div class="header box">
                <ul>
                    <li><img src="img/users/{{username}}.png" width="100px" height="auto"></li>
                    <li><p class="item bnFont"> জনাব {{first_name}} {{last_name}} - {{role_name}} (বিয়াম ফাউন্ডেশন, ঢাকা) </p></li>
                    <li>{{message}}</li>
                </ul>
                <ul>
                    <li><a class="bnFont button" href="/find_bill">&#9993; বিল সমুহ</a></li>
                    <li><a class="bnFont button" href="/info">&#10016; বৃত্তান্ত</a></li>
                    <li><a class="bnFont button" href="/logout">&#9785; লগআউট</a></li>
                </ul>
            </div>
        </div>
        <div class="content">
            <!-- Rendering -->
                {% if users is not empty %}
    <table class="admin-table bnFont">
        <tdead>
            <tr class="fr">
                <td>বিল আইডি</td>
                <td>বিবরণ</td>
                <td>খাবার বিল</td>
                <td>হল ভাড়া</td>
                <td>মোট বিল</td>
                <td>পরিশোধ</td>
                <td>বকেয়া</td>
                <td>বিল গ্রহন</td>
                <td>বিল তৈরীর তারিখ</td>
            </tr>
        </tdead>
        <tbody>
            {% for user in users %}
            <tr>
                <td>{{user.bill_id}}</td>
                <td  class="left-align">{{user.description}}</td>
                <td>{{user.food_bill}}</td>
                <td>{{user.hall_rent}}</td>
                <td>{{user.total_amount}}</td>
                <td>{{user.paid_amount}}</td>
                <td class="red">{{user.due_amount}}</td>
                <td class="red">
                    <form action="/cashier" method="post">
                        <input type="hidden" name="csrf_token">
                        <input type="text" name="payment" placeholder="Payment" pattern="^\d+(\.\d+)?$" required>
                        <input type="text" name="mr_no" placeholder="MR NO." pattern="^\d+(\.\d+)?$" required>
                    </form>
                </td>
                <td>{{user.updated_at}}</td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% else %}
        <h4 class="admin-foot bnFont">কোন বকেয়া বিল নেই। </h4>
    {% endif %}
    <!-- End of Rendering -->
        </div>
    </div>
</body>
</html>

                    <form method="get" action="/admin_officer?hall={{bill_id}}">
                
                    </form>