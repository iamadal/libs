<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CSS-Only Donut Chart</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 0;
            text-align: center;
        }

        /* Top Nav Bar */
        .navbar {
            background-color: #333;
            color: white;
            padding: 15px;
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .navbar .user-info {
            margin-right: 20px;
        }

        .navbar .user-info span {
            font-weight: bold;
        }

        .container {
            width: 80%;
            margin: 0 auto;
            padding-top: 20px;
        }

        h1 {
            margin-bottom: 20px;
        }

        .chart-container {
            width: 30%; /* Chart size */
            margin: 20px auto;
            position: relative;
        }

        /* Donut Chart (CSS Only) */
        .donut-chart {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            background: conic-gradient(
                #4caf50 0% 60%,  /* Paid (60%) */
                #f44336 60% 90%, /* Due (30%) */
                #ff9800 90% 100% /* Review (10%) */
            );
            position: relative;
        }

        /* Inner Circle to create donut effect */
        .donut-chart::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            width: 80px;
            height: 80px;
            background-color: white;
            border-radius: 50%;
        }

        /* Percent Labels */
        .donut-chart .label {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            font-size: 16px;
            font-weight: bold;
        }

        /* Information Table */
        .info-table {
            margin: 0 auto;
            width: 50%;
            border-collapse: collapse;
            margin-bottom: 20px;
        }

        .info-table th, .info-table td {
            padding: 12px;
            border: 1px solid #ccc;
            text-align: left;
        }

        .info-table th {
            background-color: #f2f2f2;
        }

        .info-table tr:nth-child(even) {
            background-color: #f9f9f9;
        }

        .info {
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <!-- Top Navigation Bar -->
    <div class="navbar">
        <div class="logo">
            <h2>Dashboard</h2>
        </div>
        <div class="user-info">
            Logged in as: <span id="username">John Doe</span>
        </div>
    </div>

    <div class="container">
        <h1>Bills Overview</h1>

        <!-- Information Table -->
        <table class="info-table">
            <thead>
                <tr>
                    <th>Category</th>
                    <th>Count</th>
                    <th>Percentage</th>
                </tr>
            </thead>
            <tbody>
                <tr>
                    <td>Total Number of Bills</td>
                    <td id="totalBills">100</td>
                    <td>100%</td>
                </tr>
                <tr>
                    <td>Paid Bills</td>
                    <td id="paidBills">60</td>
                    <td>60%</td>
                </tr>
                <tr>
                    <td>Due Bills</td>
                    <td id="dueBills">30</td>
                    <td>30%</td>
                </tr>
                <tr>
                    <td>Bills Under Review</td>
                    <td id="reviewBills">10</td>
                    <td>10%</td>
                </tr>
            </tbody>
        </table>

        <!-- Donut Chart -->
        <div class="chart-container">
            <div class="donut-chart">
                <div class="label">Bills</div>
            </div>
        </div>
    </div>

</body>
</html>
