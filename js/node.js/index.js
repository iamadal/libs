const app = require('http')

http.createServer((request,response)=>{
    request.write("This is simply hired")
    request.write("is being processed")
    request.write("Flow is good")

    for(imax=0;imax<=100;imax+=1){
       if(reponse < 10) set = 12;
       if(reponse > 10) set = 12;

    }
})