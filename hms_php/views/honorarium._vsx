<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Honorarium Management System</title>
    <link rel="stylesheet" href="css/styles.css">
   

    <link rel="icon" type="image/png" sizes="32x32" href="fav.png">
</head>
<body>
  <div class="dashboard box">
      <div class="dashboard-header">
        <div class="right">
            <img src="img/biam.png" width="55px" height="auto">
        </div>
        <div>
            <p class="header-text">Honorarium Management System v1.0</p>
            <sub>User: {{ user_info }}</sub> 
            <p>{{ course_id }}: {{description}} </p>
        </div>
        <div class="left">
          <a href="/dashboard" class="button">Dashboard </a>
          <a href="/honorarium" class="button">Add New </a>
          <a href="/honorarium?all=1" class="button">Speakers</a>
          <a href="/honorarium?hl=all" class="button">Honoraria</a>
          <a href="/logout" class="button bg-brown">Logout &#9732;</a>
        </div>
      </div>

     {% if hl == 0 %}
        <div class="hcontent">
           <!-- Add Honorarium -->
           <br>
           <p align="center">Add New honorarium for: {{description}}</p> 
           <br>
           <div class="add-honorarium">
              
              <input id="speaker_name" type="text" name="speaker"  placeholder="Search Speaker from here.." autocomplete="off" required>
              <div id="suggestions" class="suggestions"></div>
              <form class="warp box" action="/honorarium" method="get">
               <br>  <p align="center">Please insert speaker info. from the search box or insert your own. </p><br>
              <b>Speaker Name:  <input id="speaker" type="text" name="speaker_name"  placeholder="Enter Speaker name(Manual Entry or User Search Box..)" required> <br>
                Speaker ID:<input id="sid" type="text" maxlength="4" name="sid"      placeholder="Enter 4 digit Speaker ID(Not applicable for automatic entry) " pattern="^\d{4}$" required> <br>
                Designation:<input id="designation" type="text" name="Designation" placeholder="Designation(e.g Prof. , Asst. Prof)" required><br>
                Office/Institute:<input id="office" type="text" name="office"      placeholder="Institution(e.g University of Dhaka)" required><br>
                Honorarium(BDT):<input id="honorarium" type="text" name="honorarium" maxlength="4" pattern="^\d{4}$"   placeholder="Honorarium Per Hour(e.g 4600)" required><br><br>
                Topic: <input type="text"  name="topic" placeholder="Enter the topic of lecture" required>
                <label>Date of Lecture:</label> <input type="date" name="lecture_date" required>
                <label>Start Time:</label>  <input type="time"  name="cst" required>
                <label>End Time:</label>    <input type="time" name="cet"  required></b>
              <input type="submit" value="Save" class="button">
           </form>
           <br><br>
           </div>
      <hr>
  </div>
     {% endif %}

   {% if hl == 1 %}
     <div class="search"><form><input type="text" name="search" placeholder="Search honorarium.."><input class="button" type="submit" value="Search" ></form></div>
    <table class="honorarium-table">
    <thead>
        <tr>Total Honorarium: {{total}}</tr>
        <tr><td colspan="2"></td></tr>
        <tr>
            <th>Speaker ID</th>
            <th>Name</th>
            <th>Description</th>
            <th>Topic</th>
            <th>Date</th>
            <th>Start</th>
            <th>End</th>
            <th>Duration</th>
            <th>Honorarium</th>
            <th>Payment</th>
            <th>REMOVE</th>
        </tr>
    </thead>
    <tbody>
        {% for record in hr %}
            <tr>
                <td>{{ record.speaker_id }}</td>
                <td>{{ record.Name }}</td>
                <td>{{ record.Description }}</td>
                <td>{{ record.Topic }}</td>
                <td>{{ record.Lecture_date }}</td>
                <td>{{ record.CST }}</td>
                <td>{{ record.CET }}</td>
                <td>{{ record.Duration }}</td>
                <td>{{ record.Honorarium |number_format(0, '.', ',')}}/-</td>
                <td>{{ record.Payment }}</td>
                <td><a class="button" href="/honorarium?delete={{record.speaker_id}}">DELETE</a></td>
            </tr>
        {% endfor %}
    </tbody>
</table>
   {% endif %}
</body>
 <script type="text/javascript" src="js/script.js"></script>
</html>
